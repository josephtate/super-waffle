name: Create Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.6"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          make dev

      - name: Verify tag version matches package version
        run: |
          # Get version using pkg_resources
          PKG_VERSION=$(python -c "import pkg_resources; print(pkg_resources.get_distribution('rlc.cloud-repos').version)")
          # Extract tag version (remove 'v' prefix)
          TAG_VERSION=${GITHUB_REF#refs/tags/v}

          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "Version mismatch: Tag version ($TAG_VERSION) does not match package version ($PKG_VERSION)"
            exit 1
          fi

      - name: Build distribution
        run: |
          make sdist
          make dist

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/*.tar.gz
            dist/*.whl
          generate_release_notes: true
